{
  "name": "capistrano_tasks",
  "version": "0.0.2",
  "description": "some task for dnet capistrano",
  "author": "itapps",
  "scripts": {
    "release": "standard-version --release-as patch",
    "minor-release": "standard-version --release-as minor",
    "major-release": "standard-version --release-as major",
    "first-release": "standard-version --first-release"
  },
  "repository": {
    "type": "git",
    "url": "git@repo.dwp.io:itapps/capistrano_tasks.git"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0",
    "standard-version": "^4.3.0"
  }
}
